name: My First CI PIPELINE
on: 
  push:
  workflow_dispatch:
jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Code Copy Karo
        uses: actions/checkout@v4

      - name: ek message print karo
        run: echo "GitHub Actions is working perfectly!"

