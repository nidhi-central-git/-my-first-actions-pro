name: learning variables and secrets
on:  [workflow_dispatch]
jobs:
  secrets_check:
     runs-on: ubuntu-latest
     steps:
       - name: print secret
         run: |
           echo "mera secret h: ${{ secrets.MY_SECRE_MSG }}"

          
